each item in item_list
  if item.showHeading 
    .infoHeading
      if item.subsection
        h3 #{item.name}
      else
        h2 #{item.name}
  .infoBody
    | !{item.bodyHtml}
  
else
  li There are no informative sections to display.